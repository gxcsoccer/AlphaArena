web:
  url: "https://alpha-arena.vercel.app"

tasks:
  - name: "检查部署状态"
    flow:
      - ai: "访问首页"
      - sleep: 3000
      - aiQuery: "提取页面标题、主标语、以及页面显示的主要内容"
      - aiAssert: "页面显示的标题是 AlphaArena 且是算法交易平台，而不是 Trade Against Smart Agents"
